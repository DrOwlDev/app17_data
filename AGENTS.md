## Learned User Preferences

- Prefer a dense Windows UI: tighter spacing, smaller market heading fonts, no top banners (e.g. remove "Low Temp Markets") that waste vertical space; default backgrounds white (scaffold/cards)—use colored fills only when explicitly requested (e.g. convergence thresholds).
- Avoid redundant labels; if a degree already shows its percentage (e.g. "14° 99%"), do not also show a separate "% Yes" style label; format top outcomes as "x% @ yC" (e.g. "70% @ 26C").
- Always hide markets past local EOD (no toggle), sort by time-to-EOD, always auto-refresh every 3 minutes (no checkbox), show all active market dates (no date filter); no Min convergence or Min time-to-EOD filters (removed).
- Outcome percentages and Buy Yes / Buy No must match Polymarket’s live display (including "-" / unavailable when Polymarket shows no odds); chance % is Polymarket’s large percentage—on wide bid-ask prefer mid over a stale last trade when Polymarket shows mid (e.g. 29% vs last 68%), not Buy Yes ask.
- Accordion expand: only one market open at a time; no blue city badge; when expanded, show Buy Yes and Buy No; refresh that market’s odds on expand (and via a per-market refresh control); show Settlement HUD with full English labels and hover tooltips (Observed minimum/maximum, Forecast remaining, Leading outcome) and mark physics-eliminated outcomes as "Eliminated" (not "Dead"); in-app alerts for new lower obs min / higher obs max (expanded charts) and newly appearing lock-with-No opportunities.
- Color rules: date badges vary by calendar day but never red/green/yellow/blue; EOD badge shows duration only (no "to EOD" suffix)—red if <1h, orange if <3h, else blue; market row / top-outcome chip / temperature rows use ≥95% green, ≥90% yellow, ≥80% cyan, else white.
- Strategy dropdown at the top of the filter bar: "Show All" vs "Find Locked Market (≥ 90%) with No's Opportunities" (at least one temperature with chance ≥ 90%, and at least one other with chance < 90% where Buy No > 1¢ and not "--") vs "Find Markets with only 1 Buy Yes >95c" (exactly one temperature with Buy Yes ≥ 95¢); default to the locked-with-Nos strategy on startup.
- Type dropdown above Strategy: "Low" (lowest markets only) vs "High" (highest only) vs "Low & High" (all); default Low.
- "Hide thin rows" on by default on the same row as Search City: hide outcomes only when Buy Yes < 1¢ and Buy No is "--" (do not filter by volume or chance <1%).
- "Hide non-Min/Max table rows" on by default: filters the temperature points table to Extreme Min/Max plus all Forecasted rows (hides non-extreme Observed only); does not change chart points.
- Multi-tab UI: "Temperature Markets" (unified lowest + highest daily temperature markets with Low/High chips), "Sites" (city directory), and "Current Positions"; positions show live chance + city-local EOD when joined to the markets cache, and tap opens/expands that market on Temperature Markets; on the web build, include a link to the GitHub Actions refresh workflow so data refresh can be triggered manually.
- Hong Kong: HKO checkbox next to Search City — when checked: search "Hong Kong", Type "Low & High", Strategy "Show All"; when unchecked: clear search, Type "Low", Strategy locked-with-Nos. Hong Kong markets only also show an icon/control that opens the HKO regional portal `https://www.hko.gov.hk/en/wxinfo/awsgis/regional_portal.html?loc=hko` (do not add that portal control for other markets).

## Learned Workspace Facts

- **Primary purpose (app17_data):** GitHub Actions temperature **archive** — append observed temps + daily obs min/max per chartable Polymarket station, and snapshot 72h hourly forecasts as CSVs under `data/stations/` about every **30 minutes** (same job as observed). **Prune** files older than **90 days**. Build `data/analysis/skill.json` + `files_manifest.json`. Deploy static **GitHub Pages** from `site/` + `data/` (`index.html` file browser with downloads; `skill.html` forecast-skill UI). Collectors: `tool/collect_stations.dart`, `collect_observed`, `collect_forecasts`, `prune_archive`, `build_skill_stats`, `build_pages`. Workflow `.github/workflows/collect-archive.yml`. Temps in °C. Skill: pair forecast↔obs within ±30m; hit @ 0.5°C; time-to-skill \(L\). Drive upload later.
- This repo still contains the Flutter Polymarket daily temperature weather markets UI (`app17_data`, lowest Gamma tag `104597` + highest `104596`): Windows and Android use live Gamma/CLOB; GitHub Pages web reads same-origin static `data/markets.json` (legacy). The snapshot also preloads WRH, Weather Underground airport ICAO (e.g. Jinan ZSJN, Taipei RCSS), and Hong Kong HKO temperature series. City names parse from both “Lowest” and “Highest temperature in … on …” titles. Android applicationId is `dev.drowl.app17`; release signing uses local `android/key.properties` + keystore (gitignored).
- Manual Refresh and the 3-minute auto-refresh must reload markets and odds from Polymarket while preserving filters and the selected market (Windows/Android live path).
- "Time to EOD" is hours and minutes until 23:59 in the market city’s local timezone—not market close/resolution time and not a stale calendar-day "EOD passed" when local evening still remains.
- Temperature-outcome / chance stats must stay consistent with Polymarket’s site (wrong % or inventing a winner when Polymarket shows "-" is a bug); on wide spreads prefer mid when that matches Polymarket’s large %, over Buy Yes ask or a conflicting last trade alone.
- Current Positions loads the Polymarket Data API for the configured public proxy wallet `0x8cEF3c1B592953D61EEE2bC9375C5944A8926B6d`.
- Sites tab lists unique cities A–Z with each city’s resolution-source URL (from Gamma `resolutionSource` or Rules/description); show °C/°F badges from outcome labels; for `weather.gov/wrh/timeseries` sources title as `City - siteId` (e.g. `Guangzhou - zggg`); for WU `/history/daily/.../{ICAO}` title as `City - ICAO` but keep the red "Unique Resolution Source" badge; otherwise show that badge alone.
- Opening weather.gov WRH timeseries links for °C markets appends `&units=metric`; °F markets leave the URL unchanged.
- Expanded Temperature Markets rows show a temperature chart for WRH timeseries sites, Weather Underground airport ICAO markets (METAR + Open-Meteo via `StationTemperatureApi`), and Hong Kong (HKO): observed before now, forecast after, city-local from 00:00 through next-day 00:00 inclusive; temperature table places Extreme to the right of Temp. Unique-source chart paths (HKO, WU ICAO) must not change WRH timeseries behavior. Settlement HUD Obs min / Obs max / Eliminated use only real `TempPointKind.observed` points—never forecast points even when `markDailyExtremes` sets `isDailyMinimum`/`isDailyMaximum` (chart stars may still use forecast extrema). Low: warmer exact buckets die on obs min; High: colder exact / “or below” floor buckets die when obs max already exceeds them.
- Hong Kong charts use `hkoc.csv` observed (minute resolution, HKT) + OCF `HKO.xml` forecast + latest 1-min CSV for the now-line label; settlement reference remains HKO Daily Extract Absolute temperatures (not scraped for the live curve). Hong Kong forecast charts also show HKO-style weather icons above the yellow forecast series only (from OCF `ForecastWeather`, ~3-hourly; observed side stays temperature-only).
- Jinan/Taipei (and similar) settle on Weather Underground Daily Observations for airport ICAOs; charts prefer aviationweather.gov METAR for that ICAO as observed, then Weather.com historical (same WU table) when AWC returns no METARs (e.g. ZSJN), plus Open-Meteo at airport lat/lon as forecast (NWS unavailable outside US)—do not route through HKO; prefer API/historical feeds over headless scraping of wunderground.com.
- Forecast preference for WRH charts: `api.weather.gov` hourly first, then Open-Meteo NBM (`ncep_nbm_conus`) for US fallback, then default Open-Meteo as last resort / non-US. Archive 72h snapshots use the same cascade with Open-Meteo `forecast_days=4`.
- WRH/HKO chart styling: observed temps black at native station resolution (sub-hourly when available); forecast temps yellow; blue min / orange max horizontal lines and stars with labels at full available decimal precision; red vertical now-line labeled with latest station observation temp and time when available.
- Archive APIs: `StationTemperatureApi.fetchRecentObservations` / `fetchForecastHorizonC`; `HkoTemperatureApi` equivalents; CSV helpers in `lib/archive/station_archive.dart`.
